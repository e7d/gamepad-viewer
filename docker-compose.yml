version: "3.3"

services:
    web:
        image: nginx
        ports:
            - 8081:80
        volumes:
            - .:/usr/share/nginx/html:ro
            - ./docker/default.conf:/etc/nginx/conf.d/default.conf:ro
